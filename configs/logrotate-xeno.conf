# XENO.net cascade logs — keep disks from filling
/var/log/xeno/*.log
/var/log/xeno/events.jsonl {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    maxsize 100M
}
